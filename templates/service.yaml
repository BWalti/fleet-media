apiVersion: v1
kind: Service
metadata:
  name: {{ include "fleet-media.fullname" . }}
  labels:
    {{- include "fleet-media.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fleet-media.selectorLabels" . | nindent 4 }}
