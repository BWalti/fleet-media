apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fleet-media.fullname" . }}-test-connection"
  labels:
    {{- include "fleet-media.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fleet-media.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
